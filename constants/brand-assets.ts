/**
 * Brand images live under `assets/icons/` (project root).
 * Same file is referenced in `app.json` → `expo-splash-screen` → `image`.
 */
export const centiLogoYellow = require('@/assets/icons/LogoYellowPNG.webp');
